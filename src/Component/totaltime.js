import React from "react";
import { TimeContainer } from "./styled-components";

const TotalTime = ({ total }) => {
  return <TimeContainer>Total Time: {total}</TimeContainer>;
};
export default TotalTime;
